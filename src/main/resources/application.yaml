spring:
  application:
    name: noticeboard-V3

  datasource:
    url: jdbc:mysql://localhost:3306/noticeboardV3?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: sunwo0430          # 본인 MySQL 계정
    password: sun0430sun         # 본인 MySQL 비밀번호
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update      # 엔티티 보고 테이블 자동 생성/수정 (학습용으로 편함)
    show-sql: true          # 실행되는 SQL을 콘솔에 출력 (N+1 확인할 때 유용!)
    properties:
      hibernate:
        format_sql: true    # SQL을 보기 좋게 줄바꿈 (지난번에 물어봤던 그 설정)